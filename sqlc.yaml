version: "2"
sql:
  - engine: "postgresql"
    schema: "migrations"
    queries: "queries"
    gen:
      go:
        package: "queries"
        out: "pkg/database/queries"
        sql_package: "database/sql"
        emit_json_tags: true
        emit_empty_slices: true
